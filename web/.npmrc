# pnpm 配置文件
# 确保跨平台一致性

# 自动安装 peer dependencies
auto-install-peers=true

# 严格的 peer dependencies 检查（警告但不阻止）
strict-peer-dependencies=false

# 使用扁平化的 node_modules 结构（提高兼容性）
shamefully-hoist=true

# 确保锁文件始终更新
prefer-frozen-lockfile=false
